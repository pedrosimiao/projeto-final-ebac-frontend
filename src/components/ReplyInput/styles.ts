// src/components/ReplyInput/styles.ts

import styled from "styled-components";

export const ReplyContainer = styled.div`
  border-bottom: 1px solid ${({ theme }) => theme.colors.border};
  display: flex;
  flex-direction: column;
`;
